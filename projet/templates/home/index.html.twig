<!DOCTYPE html>

{% block import_css %}
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
{% endblock %}

<title>Accueil</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Accueil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/sujet">Forum <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
            {% if is_granted('ROLE_ADMIN') %}
            <a href="/admin/dashboard/" class="nav-link">Dashboard</a>
            {% endif %}
        </li>
      </ul>
      <ul class="navbar-nav mr-1">
        <li class="nav-item active">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="/login" class="nav-link">Se connecter</a>
            {% else %}
            <a href="/register" class="nav-link">Créer un compte</a>
            {% endif %}
        </li>
      </ul>
    </div>
</nav>

<div class="example-wrapper">
    <h1>Bienvenue sur le site! ✅</h1>

    N'oubliez pas d'aller créer un compte ou de vous connecter sinons vous ne pourrez pas aller sur le forum :
    <ul><br>
        {% if is_granted('IS_AUTHENTICATED_FULLY') or is_granted('ROLE_ADMIN') %}
        <li>Se deconnecter -> <a href="/logout">Se déconnecter </a></li>
        {% else %}
        <li>Aller vous créer un compte -><a href="/register">Création de compte</a></li><br>
        <li>Aller vous connectez -><a href="/login">Connexion</a></li>
        {% endif %}
    </ul>
</div>
{% endblock %}
